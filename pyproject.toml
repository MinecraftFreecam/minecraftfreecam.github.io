[project]
name = "freecam-website"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "properdocs>=1.6.7",
    "mkdocs-materialx>=10.2.0",
]

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "ruff>=0.16.5",
]

[tool.uv]
package = false
