[project]
name = "freecam-website"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "properdocs>=1.6.7",
    "mkdocs-materialx>=10.1.4",
]

[dependency-groups]
dev = [
    "mypy>=1.20.1",
    "pytest>=9.0.0",
    "ruff>=0.15.6",
]

[tool.uv]
package = false
