[project]
name = "freecam-website"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs>=1.4",
]

[dependency-groups]
dev = [
    "mypy>=1.19.0",
    "pytest>=9.0.0",
    "ruff>=0.15.6",
]

[tool.uv]
package = false

